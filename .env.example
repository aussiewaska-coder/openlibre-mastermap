# AWS Marketplace Terrain Tiles Configuration
# The default configuration uses the public AWS S3 bucket (elevation-tiles-prod)
# If you need authenticated access or a different region, configure these variables:

# Optional: AWS Region (default: us-east-1)
# AWS_REGION=us-east-1

# Optional: AWS Access Key ID (only needed if bucket requires authentication)
# AWS_ACCESS_KEY_ID=your-access-key

# Optional: AWS Secret Access Key (only needed if bucket requires authentication)
# AWS_SECRET_ACCESS_KEY=your-secret-key

# Optional: Custom DEM tile URL template
# DEM_TILE_URL=https://elevation-tiles-prod.s3.amazonaws.com/{z}/{x}/{y}.png
